<%= content_for :head do %>
  <%= javascript_include_tag 'admin/schedules/assignments' %>
<% end %>
<div id='schedule_title'>
  <%= render :partial => 'schedule_title', :locals => {:s => @object} %>
</div>
<div>
  <div class='toolbar'>
    <ul class='actions'>
      <li>
      <%= button_link_to 'Add bus', new_admin_schedule_schedule_assignment_path(@object) + "?schedule_id=#{params[:id]}", { :remote => true, :icon => 'bus'} %></li>
      <li><%= button_link_to 'Hold', 'javascript:hold()', {:icon => 'hold'} %></li>
      <li><%= button_link_to 'Release', 'javascript:release();', {:icon => 'undo'} %></li>
      <li><%= button_link_to 'Order', 'javascript:order();', {:icon => 'cart'} %></li>
    </ul>


    <br class='clear' />
  </div>

  <div id="tabs">
    <ul class='tabs'>
      <% @object.assignments.each do |assignment| %>
        <li><%= link_to "#{assignment.id}:#{assignment.title}", admin_schedule_schedule_assignment_bus_seats_path(@object, assignment) %></li>
      <% end %>
    </ul>
  </div>
  <!-- single pane. it is always visible -->
  <div class="panes">
    <% @object.assignments.count.times do %>
      <div style='display:none'></div>
    <% end %>
  </div>

</div>

<div>
  <table class="index">
    <tr>
      <th><%= t("order_id") %></th>
      <th>Type</th>
      <th>Related id</th>
      <th><%= t("tour") %></th>
      <th>Departure</th>
      <th>Customer</th>
      <th>Phone</th>
      <th>Status</th>
      <th>Op</th>
    </tr>
    <% @object.orders.each do |order| %>
      <tr id="<%= dom_id order %>" class='<%= cycle('row', 'alt-row') %>'>
        <td style="font-weight:bold;text-align:center;text-decoration:underline;">
          <%= link_to order.id, admin_order_path(order) + "/edit?p=order_seats", :remote => true %>
        </td>
        <td><%= order.order_source_type %></td>
        <td><%= order.order_source_id %></td>
        <td></td>
        <td></td>
        <td><%= order.order_customer.full_name if order.order_customer %></td>
        <td>phone</td>
        <td><%= order.status %></td>
        <td><%= link_to 'seat', 'javascript:assign_seat();' %></td>
      </tr>
    <% end %>
  </table>
</div>

<div id='edit_div' class='dialog-normal'></div>

