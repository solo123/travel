var api = $('.tabs').data('tabs');
api.getCurrentPane().load(api.getCurrentTab().attr('href'));

$('#unseat_orders').html("<%=j render '/admin/schedules/orders', :orders => @object.schedule.orders %>")

